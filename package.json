{
	"private": true,
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"watch": "tsc -p tsconfig.build.json -w"
	},
	"devDependencies": {
		"@types/node": "latest",
		"@volar/language-service": "next",
		"typescript": "latest"
	}
}
